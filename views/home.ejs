<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= rest.name %></title>
</head>
<body>
  <h1><%= rest.name %></h1>
  <h3>Address: <%= rest.address %></h3>
  <h3>Phone: <%= rest.phone %></h3>
    <% if (rest.isOpen){ %>
        <p>Yes we are open!</p>
    <% } else { %>
        <p>Sorry, we are closed.</p>
    <% } %>
</body>
</html>
